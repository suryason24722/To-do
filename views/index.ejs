<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <link rel="stylesheet" href="/stylesheets/style.css">
        <script src="https://cdn.tailwindcss.com"></script>
    </head>

    <body>

        <div class="main w-full min-h-screen bg-zinc-900 border-2 border-green-500 p-2">
            <h1 class="text-blue-500 text-5xl m-9">Welcome to a simple to-do list</h1>

            <div class="form p-10 border-2 border-red-500 text-white">
                <form class="mx-auto" action="/create" method="post">
                    <input class="block w-full rounded-lg outline-none px-5 py-5 bg-zinc-800" type="text"
                        placeholder="Write your Task title here..." name="title">
                    <textarea class="block w-full rounded-lg outline-none px-5 py-2 mt-2 bg-zinc-800 resize-none"
                        placeholder="Write your task details here..." name="details"></textarea>
                    <input class="border-2 mt-2 bg-blue-500 px-5 py-2 rounded-md " type="submit" value="create task">
                </form>
            </div>

            <div class="tasks w-full border-2 mt-2 p-10 flex gap-3 flex-wrap">

                <% if(files.length>0){ %>
                    <% files.forEach(function(val){ %>  
                        <div class="task w-72 px-3 py-4 rounded-lg bg-zinc-800 ">
                            <h1 class="text-white text-3xl tracking-tighter">
                                <%= val %>
                            </h1>
                            <a class="text-blue-500 inline-block mt-3" href="/file/<%= val %>">Read more</a>
                        </div>
                        <% }) %>
                            <% } else{ %>
                                <h3 class="text-zinc-500">no tasks yet.</h3>
                                <% } %>




            </div>

        </div>
        <script src="/javascripts/script.js"></script>
    </body>

</html>